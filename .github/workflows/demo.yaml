name: Hello-World

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello build
        run: echo "This is build stage"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello test
        run: echo "This is test stage"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Hello deploy
        run: echo "This is deloy stage"
    
